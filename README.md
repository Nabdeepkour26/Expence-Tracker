# Expense Tracker

A simple Python expense tracker script that allows you to:

- add expense amounts
- view recorded expenses
- calculate the total expense amount

## Usage

Run the script with Python:

```bash
python "expences tracker.py"
```

Then choose from the menu:

1. Add expense
2. View expenses
3. Total Amount
4. Exit
